#pragma once
#include <iostream>
#include <vector>
#include <memory>
#include "User.h"
#include "Message.h"

class Chat
{
private:
	//std::vector<User> _users;
	//std::vector <Message> _messages;

	

public:
	
	

	
	
	/*


	void register_user(std::string lgn, std::string pwd)
	{
		_users.emplace_back(lgn, pwd);
	}
	void get_user(int number)
	{
		std::cout << _users[number] << std::endl;
	}
	/*void login_busy(std::string lgn)
	{
		
		for (auto i : _users)
		{
			if (lgn == getLogin->_users[i])
		}
	}

	
	
	
*/
};
